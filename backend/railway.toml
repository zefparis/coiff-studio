# railway.toml - Backend (Node.js + Express + SQLite)

[build]
# Install dependencies and prime better-sqlite3
command = "npm install"

[deploy]
# Start the Express server via package script
startCommand = "npm run start"

[[volumes]]
# Persistent storage for the SQLite database
name = "salon-db"
mountPath = "/data"
